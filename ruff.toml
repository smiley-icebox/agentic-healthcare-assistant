# Lint config. Default rule set (pyflakes F + pycodestyle E4/E7/E9) catches the things
# that actually matter — undefined names, unused imports, real style errors — without
# fighting the deliberately wide comment lines in this codebase.
line-length = 100
target-version = "py312"
extend-exclude = [".venv"]
